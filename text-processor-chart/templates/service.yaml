apiVersion: v1
kind: Service
metadata:
  name: {{ include "text-processor-chart.fullname" . }}
  labels:
    {{- include "text-processor-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "text-processor-chart.selectorLabels" . | nindent 4 }}
